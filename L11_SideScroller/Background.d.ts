declare namespace L11_SideScroller {
    import f = FudgeCore;
    class Background extends f.Node {
        constructor(_name: string, _backgrounds: HTMLImageElement[], _zDistance?: number);
    }
}
